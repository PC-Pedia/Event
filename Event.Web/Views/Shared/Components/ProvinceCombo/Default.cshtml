@model Dictionary<int,string>

<select class="form-control" id="@ViewBag.Name" name="@ViewBag.Name">
    <option value="">استان</option>
    @{ 
        foreach (var item in Model)
        {
            <option value="@item.Key">@item.Value</option>
        }
    }
</select>

